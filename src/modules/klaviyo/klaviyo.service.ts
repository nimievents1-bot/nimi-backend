// Removed — Klaviyo integration was replaced by direct Resend sends + cron
// triggered endpoints (see `mailer/indulgence-templates.ts` and `cron/`).
// This file remains as a deletion marker so the diff reads cleanly; it can
// be removed entirely on the next clean-up pass.
export {};
